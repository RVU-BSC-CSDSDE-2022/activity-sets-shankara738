#include <stdio.h>
int main(void)
{
 int a,b,c,sum;
  printf("enter the first number");
  scanf("%d",&a);
  printf("enter the second number");
  scanf("%d",&b);
  sum=a+b;
  printf("the sum of 2 numbers is %d",sum);

  return 0;
}